# NORTHSTAR — ha-pp-consensus

**DONE** = a proposal reaches decided status with explicit ACK from both pp-docs and ppd-arch (or tie-break noted), written to decisions/ + consensus.jsonl.

Core KPI: clean scope gates without conductor solo decisions.

- All CORE-SURFACE / wave GO go through consensus gate.
- Evidence files: proposal json, .dev.json .docs.json, decision json.
- Bus + repo mirrors updated.
- Selftest PASS + workflow runs end-to-end.
- No theater: only real ACKs from the two sides.
