# COLLAB — ha-pp-consensus

Shared bus for cross-team collab (e.g. with ha-pumapay / ha-ppdev).

Bus root: `~/.grok/pumapay-bus/consensus/`

Use consensus.py or ctl for propose/verify. Append via consensus.jsonl.

Lanes coordinate via proposals/decisions + shared pumapay-bus.

See CONSENSUS.md for protocol.
