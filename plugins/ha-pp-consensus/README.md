# ha-pp-consensus

```bash
bash ~/.grok/skills/ha-pp-consensus/scripts/ctl.sh selftest
bash ~/.grok/skills/ha-pp-consensus/scripts/ctl.sh tick   # status only — no vote
```

Closed: `c-20260912-repo-estructura-r1` ACK×2. Do not propose unless the operator asks.
