# ha-payments

Reusable product team. Canonical live copies:

- Agents: `$GROK_HOME/agents/pay-*.md` (includes `pay-lead`)
- Skill: `$GROK_HOME/skills/ha-payments/`
- Workflow: `$GROK_HOME/workflows/ha-payments.rhai`
- Conductor: `$GROK_HOME/skills/ha-payments/references/AUTONOMY.md` + `scripts/ctl.sh`

This folder is the marketplace mirror. Sync from `$PUMAPAY_ROOT/equipos/`.

Purpose: PumaPay payments: CORE-SURFACE rails, sandbox PSP, signed webhooks.
Profile: FINTECH-BUILD. Domain contracts; ha-ppdev implements.
