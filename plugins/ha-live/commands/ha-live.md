---
name: ha-live
description: Start or continue the HA runtime build (Mac ha-live + AMS /opt/ha-live).
---

Cwd `/Users/c/dev/ha-live`. Model grok-4.6. Follow `~/.grok/skills/ha-live/SKILL.md`.

If this chat is not already in that repo: tell the operator to `cd /Users/c/dev/ha-live` and start a **new** grok-4.6 session with HA, then paste `PROMPT.md` — or, if cwd is already ha-live, execute `PROMPT.md` now (read BUILD.md, preflight, ctl start, prove.sh).

Do not work this job from Desktop/puma. Do not revive autonomy-core.
