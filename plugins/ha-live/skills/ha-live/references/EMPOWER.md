# Empowerment pack (added while the TUI was already running)

The remainder + board already exist. This pack is the **muscle** the TUI can call without inventing lanes.

| Call | What |
|------|------|
| `/ha-live-tick` | Workflow: snapshot → scout ∥ pivot ∥ learn → `verify_board.py` |
| `/ha-live-engage` | `scripts/engage.sh <id>` → ha-hackers only if AMS proxy GO |
| spawn `ha-live-scout` | P3 first-party AMS/xray |
| spawn `ha-live-pivot` | P3 next host from disk; no Mac ISP |
| spawn `ha-live-learn` | P2 capability from a file that exists |

If Grok shows **two** `/ha-live-tick`: pick the **Command** whose text is “USE THIS — one organism tick”. Never `/ha-live-boot` while the unit is active.

`/ha-live-ralph` = repeat tick in **this** TUI until the board is quiet.

Escape hatch third-party (burns AMS IP, not Mac ISP):

```bash
bash scripts/ams-direct-probe.sh && HA_AMS_PROXY=direct bash scripts/ctl.sh start
```

Law: `BOARD.md` `CONTINUE.md` `proof/BLOCKER.md`.
