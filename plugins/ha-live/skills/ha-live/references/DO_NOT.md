# Do not

- `/Users/c/Desktop/puma` — accidental cwd, a target, not the organism
- `/Users/c/dev/autonomy-core` — stale (2026-08-04), Fusion-spam caps, **not** the architecture to revive
- `/opt/huhu-cloud` — C2 panel, other product
- Treat Terminaitor four-pillar docs as gospel. Intent yes. Tick/746-caps/hive-as-written **no**
- Nested `grok -p`
- Wait for Kimi
- Ship a prompt-only “lane” as the runtime
- Invent GO. Theater. Skeleton. Fake live
- Egress third-party from Mac ISP
- Assume AMS `127.0.0.1:10808` is the Mac SOCKS
- Money-write (deposit/payout/approve/drain) unless the operator **named** it this run
- Replace `runtime/remainder.py` with a different organism “because Terminaitor”
- Call a Mac-only python loop “AMS remainder”
