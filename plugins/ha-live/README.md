# ha-live plugin

Grok plugin layout: `agents/`, `skills/`, `commands/`, `workflows/`.

User-level (also copied here):

- `~/.grok/workflows/ha-live-boot.rhai`
- `~/.grok/personas/ha-live-*.toml` `~/.grok/roles/ha-live-*.toml`
- `~/.grok/agents/ha-live-*.md`

Install: copy to `~/.grok/plugins/ha-live` (auto-trusted). Enabled in `~/.grok/config.toml` `[plugins].enabled`.

Work surface: `/Users/c/dev/ha-live`. AMS: `/opt/ha-live`.
