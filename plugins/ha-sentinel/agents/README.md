# ha-sentinel agents

sent-lead, sent-watch, sent-ollama, sent-audit, sent-improve, sent-release, sent-escalate, sent-collab

All duplicated to ~/.grok/agents/

Frontmatter + HA prefix + CONTRACT read.

No nested spawn. Bus + OUT source of truth.

See ../skills/ha-sentinel/SKILL.md and references/.