# ha-support

Reusable product team. Canonical live copies:

- Agents: `~/.grok/agents/sup-*.md` (includes `sup-lead`)
- Skill: `~/.grok/skills/ha-support/`
- Workflow: `~/.grok/workflows/ha-support.rhai`
- Conductor: `~/.grok/skills/ha-support/references/AUTONOMY.md` + `scripts/ctl.sh`

This folder is the marketplace mirror. Sync from those paths.

Purpose: PumaPay support AI for SDPPAY.
