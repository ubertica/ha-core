# ha-sec

Reusable product team. Canonical live copies:

- Agents: `~/.grok/agents/sec-*.md` (includes `sec-lead`)
- Skill: `~/.grok/skills/ha-sec/`
- Workflow: `~/.grok/workflows/ha-sec.rhai`
- Conductor: `~/.grok/skills/ha-sec/references/AUTONOMY.md` + `scripts/ctl.sh`

This folder is the marketplace mirror. Sync from those paths.

Purpose: PumaPay AppSec+SecOps. Distinct from AMS sentinel.
