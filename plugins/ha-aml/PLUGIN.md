# ha-aml

Reusable meta team. Canonical live copies:

- Agents: `~/.grok/agents/aml-*.md` (includes `aml-lead`)
- Skill: `~/.grok/skills/ha-aml/`
- Workflow: `~/.grok/workflows/ha-aml.rhai`
- Conductor: `~/.grok/skills/ha-aml/references/AUTONOMY.md` + `scripts/ctl.sh`

This folder is the marketplace mirror. Sync from those paths.

Purpose: AML team for PumaPay v2 fintech: transaction monitoring, investigations, SAR/STR filing, customer risk rating, rule tuning and regulatory reporting. Full HA autonomy, bus collab with fraud/risk/support, selftest PASS. Same structure and quality as ha-hackers and ha-risk.
