# ha-hackers

Reusable offensive team. Canonical live copies:

- Agents: `~/.grok/agents/hack-*.md` (includes `hack-entry`, `hack-webhook`)
- Skill: `~/.grok/skills/ha-hackers/` and `~/.grok/skills/ha-docs-entry/`
- Workflow: `~/.grok/workflows/ha-auto.rhai`, `docs-entry.rhai`, `ha-hackers.rhai`
- Conductor: `~/.grok/skills/ha-hackers/references/AUTONOMY.md` + `scripts/ctl.sh auto`
- Personas / roles: `~/.grok/personas|roles/hack-*.toml`

This folder is the marketplace mirror. Sync from those paths.
