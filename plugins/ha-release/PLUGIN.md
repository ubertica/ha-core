# ha-release

Reusable product team. Canonical live copies:

- Agents: `~/.grok/agents/rel-*.md` (includes `rel-lead`)
- Skill: `~/.grok/skills/ha-release/`
- Workflow: `~/.grok/workflows/ha-release.rhai`
- Conductor: `~/.grok/skills/ha-release/references/AUTONOMY.md` + `scripts/ctl.sh`

This folder is the marketplace mirror. Sync from those paths.

Purpose: PumaPay release train go/no-go.
