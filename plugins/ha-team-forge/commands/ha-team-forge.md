---
name: ha-team-forge
description: Factory for HA teams. Scaffold full plugin+agents+skills+workflows+ctl from brief + lanes.
---

Run via skill or workflow. See `~/.grok/skills/ha-team-forge/SKILL.md` and BUILD-SPEC.

Usage (via skill ctl):
bash ~/.grok/skills/ha-team-forge/scripts/ctl.sh forge --name ha-foo --prefix foo --purpose "..." --lanes lanes.json

Never nested grok -p. selftest required for DONE.
