# {{NAME}}

Grok plugin (xAI marketplace layout): `agents/`, `skills/`, `commands/`.

User-level (not plugin components per xai-org/plugin-marketplace):

- `~/.grok/workflows/{{NAME}}.rhai` (+ optional -tick)
- `~/.grok/agents/{{PREFIX}}-*.md`
- `~/.grok/skills/{{NAME}}/`

Children inherit the **parent** MCP (chrome-devtools, filesystem, …). This plugin does not ship `.mcp.json`.

Install: copy to `~/.grok/plugins/{{NAME}}` (auto-trusted) or `grok plugin install {{NAME}} --trust` from the HA marketplace.

Forge command: `bash ~/.grok/skills/ha-team-forge/scripts/ctl.sh forge --name {{NAME}} --prefix {{PREFIX}} ...`
Purpose: {{PURPOSE}}
