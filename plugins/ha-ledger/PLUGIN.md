# ha-ledger

Reusable product team. Canonical live copies:

- Agents: `~/.grok/agents/led-*.md` (includes `led-lead`)
- Skill: `~/.grok/skills/ha-ledger/`
- Workflow: `~/.grok/workflows/ha-ledger.rhai`
- Conductor: `~/.grok/skills/ha-ledger/references/AUTONOMY.md` + `scripts/ctl.sh`

This folder is the marketplace mirror. Sync from those paths.

Purpose: PumaPay ledger: double-entry, holds, recon, settlement.
