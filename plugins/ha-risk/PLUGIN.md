# ha-risk

Reusable product team. Canonical live copies:

- Agents: `~/.grok/agents/rsk-*.md` (includes `rsk-lead`)
- Skill: `~/.grok/skills/ha-risk/`
- Workflow: `~/.grok/workflows/ha-risk.rhai`
- Conductor: `~/.grok/skills/ha-risk/references/AUTONOMY.md` + `scripts/ctl.sh`

This folder is the marketplace mirror. Sync from those paths.

Purpose: PumaPay risk/fraud/AML rules, cases, scoring.
