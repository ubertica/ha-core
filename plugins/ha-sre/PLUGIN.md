# ha-sre

Reusable product team. Canonical live copies:

- Agents: `~/.grok/agents/sre-*.md` (includes `sre-lead`)
- Skill: `~/.grok/skills/ha-sre/`
- Workflow: `~/.grok/workflows/ha-sre.rhai`
- Conductor: `~/.grok/skills/ha-sre/references/AUTONOMY.md` + `scripts/ctl.sh`

This folder is the marketplace mirror. Sync from those paths.

Purpose: PumaPay SRE: SLOs, deploy, on-call. Complements ha-sentinel.
