# /ha-dani

Civil conductor for Daniel / Puma post-incidente. HARD ALLOW on. Not ha-hackers.

1. Read `~/.grok/agents/_ha-dani-law.md` and `/Users/c/dev/dani/DESIGN.md`.
2. Route to `ha-dani-audit` | `ha-dani-authz` | `ha-dani-handoff` | `ha-dani-ops`.
3. OUT under `/Users/c/dev/dani/out/<stream>`.
4. `bash ~/.grok/skills/<pack>/scripts/ctl.sh auto --out "$OUT"` then `workflow name=<pack>`.
5. Unclear / reunion → audit + handoff first.

Never: live money, live gplaygap patch, loot copy, ACCESS.md dump, Mac ISP.
