# ha-kyc

Reusable product team. Canonical live copies:

- Agents: `~/.grok/agents/kyc-*.md` (includes `kyc-lead`)
- Skill: `~/.grok/skills/ha-kyc/`
- Workflow: `~/.grok/workflows/ha-kyc.rhai`
- Conductor: `~/.grok/skills/ha-kyc/references/AUTONOMY.md` + `scripts/ctl.sh`

This folder is the marketplace mirror. Sync from those paths.

Purpose: PumaPay KYC/onboarding; AI recommend human decide.
