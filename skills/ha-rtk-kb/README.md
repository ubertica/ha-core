# ha-rtk-kb

One knowledge base. Two teams.

- Civil: `/ha-redteam` → `/Users/c/dev/dani/out/redteam`
- Offense: `/ha-blackhat` → engagement OUT (`HA_BLACKHAT_OUT`)
- Brain: `~/.grok/ha-rtk-kb/`

Redteam can search what blackhat indexed (redacted fps). Blackhat can search civil findings. Neither reads the other's loot.
