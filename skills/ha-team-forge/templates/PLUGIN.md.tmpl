# {{NAME}}

Reusable {{CATEGORY}} team. Canonical live copies:

- Agents: `~/.grok/agents/{{PREFIX}}-*.md` (includes `{{PREFIX}}-lead`)
- Skill: `~/.grok/skills/{{NAME}}/`
- Workflow: `~/.grok/workflows/{{NAME}}.rhai`
- Conductor: `~/.grok/skills/{{NAME}}/references/AUTONOMY.md` + `scripts/ctl.sh`

This folder is the marketplace mirror. Sync from those paths.

Purpose: {{PURPOSE}}
