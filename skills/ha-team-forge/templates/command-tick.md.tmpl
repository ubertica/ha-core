---
name: {{NAME}}-tick
description: Periodic tick for {{NAME}}. Remainder + lead.
---

Run tick conductor. Remainder lanes only + lead.

`bash ~/.grok/skills/{{NAME}}/scripts/ctl.sh tick --out OUT`
then `workflow name={{NAME}}-tick args.out=OUT`

See main command {{NAME}}.md .
