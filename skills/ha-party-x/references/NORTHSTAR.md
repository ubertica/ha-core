# North star — party extreme (HF-bar)

Capability bar: `~/.grok/skills/ha-hackers/references/NORTHSTAR.md` (do not copy).

ha-hackers = 5 tool lanes, short pack (recon∥api → authz → verify → exploit).
**ha-party-x** = 4 billed Groks as *brains* + g1 as *hands*. Longer, deeper, adversarial.

## Delta vs ha-hackers

| Primitive | ha-hackers | ha-party-x |
|-----------|------------|------------|
| Persistence | remainder + workstreams | same + 16–22 wave campaign that resumes mid-pipeline |
| Pivot | blocked → workstream | g4 *must* open a pivot if g2/g3 stall; DONE ≠ empty remainder |
| Collective R&D | disk bus notes | round-mode brains hear each other; red-on-red before any GO |
| 0day class | one `hack-0day` lane | dedicated 0day-deep pipeline + 5 class waves inside apt-long |
| Kill chain | `hack-chain` after GO | chain is composed *and then attacked* by g4 |
| Creds / lateral | named TARGET | persist + lateral hypotheses are first-class waves, still dummy-id |
| Swarm | bounded subagents | 3 brains × N rounds, tools only on g1. No 4 TUIs. No nested `grok -p` |

## Acceptance

1. A blocked vector opens `WORKSTREAMS.jsonl`, does not print DONE.
2. No exploit wave without disk `VERIFY.json.go_count > 0`.
3. Two GOs become an ordered `chain.md`, then g4 tries to break the chain.
4. Re-run skips `READY.<wave>`.
5. Money-write still human-gated.
