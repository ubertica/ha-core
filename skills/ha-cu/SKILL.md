---
name: ha-cu
description: >
  Computer-use Mac: kimi-cu, WebBridge, peekaboo, chrome-devtools skill.
---

# ha-cu

Follow `ha-computer-use`: user owns Retina; agent owns the other display. Real Chrome (not headless, not a second browser). Never steal focus, cursor, or clipboard. Firefox or a screenshot-dir only if the operator named them. Isolation config: `~/.grok/operator/isolation.json`.

Axis: **toolkit**.
