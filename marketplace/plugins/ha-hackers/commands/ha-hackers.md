---
name: ha-hackers
description: Run the reusable offensive team against a target (recon, api, authz, exploit, lead).
---

Dispatch the hacker team. Skill: `~/.grok/skills/ha-hackers/SKILL.md`.
Need TARGET + OUT. Prefer workflow `~/.grok/workflows/ha-hackers.rhai` or spawn the five `hack-*` types as in the skill.
