# ha-rtk-kb

Shared knowledge base for `ha-redteam` (civil) and `ha-blackhat` (offense).

Canonical:

- Skill: `~/.grok/skills/ha-rtk-kb/`
- Disk store: `~/.grok/ha-rtk-kb/`
- Script: `~/.grok/skills/ha-rtk-kb/scripts/kb.py`

Lanes stay separate. Knowledge is one. No JWT/CBU/loot bodies in the shared store. Blackhat loot stays in engagement OUT. Civil pack never reads loot files — only the index.
