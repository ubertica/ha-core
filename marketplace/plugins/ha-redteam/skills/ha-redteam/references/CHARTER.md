# CHARTER — ha-redteam

Two classes. **UNBREAKABLE** cannot be bent. Everything else non-destructive may be bent **in service of the current OBJECTIVE** (“se animan a todo”).

## UNBREAKABLE (MUST NOT)

Operator charter. OPERATOR_STOP. Do not ladder these.

1. Live money (deposit/withdraw/payout/claim/force). Amount 0 is still live.
2. Live patch of `api.gplaygap.com`.
3. Invented GO (`go_count` from FINDINGS.jsonl only).
4. PII / CBU / CUIT / JWT / dash passwords in chat or OUT. Paths only.
5. Mix `dump/` `hack/` `exfil-loot/` into product OUT/repos.
6. Mac ISP → third-party. SOCKS AMS only.
7. Minors.
8. Nested `grok -p`. Children do not spawn children (Grok depth 1). **g1** honors SPAWN_REQUEST.
9. Extra Grok TUIs (party is MCP in this TUI).
10. `cancelá` / explicit operator don’t / isolation steal of Retina/clipboard.
11. Claiming live Puma/gplaygap/AgentsBet probes have no production effect.

## BENDABLE (non-destructive, objective-first)

If it is not in UNBREAKABLE and the operator did not name it MUST NOT: **bend it**.

- Lane order / skip stale READY after a jump
- Extra specialist child (budget: 8 live)
- docs-entry vs broader probe class
- Party round vs parallel; g3 down → g4 dual
- Knowledge source (CVE/KEV/OSV/GHSA/local catalog)
- Broadcast frequency / Discord kind
- Jump to sibling host **from disk evidence**
- Chain into ha-pivot (mesh/weaver/swarm/desire)
- Red-on-red (g4 refute g2) when evidence exists
- Ralph/ACT bounds inside max 5/3 unless OBJECTIVE still open
- Writing LEARN.md / nodes_commit_turn memory

Destructive = money-write, prod patch, loot mix, PII dump. Those are UNBREAKABLE, not “bold”.

## OBJECTIVE

`OUT/.bus/OBJECTIVE.json` = living XXX. g4 polls; unanimous YES+evidence = stop. Else continue (jump/pivot/spawn).
