# ha-upgrade-v1

packed_at: 2026-09-17T22:08:23.232565+00:00
source: notes
actions: 0

## Operator notes

- /learn is the agentic self-improvement loop: traces → map-reduce → report → curate → apply.
- After /learn, pack a **new** plugin `ha-upgrade-vN`. Never overwrite vN-1.
- Pack ≠ install. Apply needs `--go`. Skip `requires_confirmation`.
- g1 `/model` is TUI slash (cannot switch from inside the agent). g4 model is API seat, not a TUI.
- Party brains inherit HA as system text; xAI classifier still fires on overlay wording in the user prompt. Reframe without the wrapper.
- g3 = SuperGrok sub-2, `GROK_HOME=~/.grok3`, `grok3 login --oauth`.
- Do not dump grant blobs. Science stays on `science --hard-allow`.
