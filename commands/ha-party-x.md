# /ha-party-x

Extreme party campaign. Read `~/.grok/skills/ha-party-x/SKILL.md` and follow AUTONOMY.md.

Need `TARGET` + `OUT`. Default pipeline `apt-long`.

```
bash ~/.grok/skills/ha-party-x/scripts/ctl.sh auto --target TARGET --out OUT --pipeline apt-long
```

Then remainder loop in this TUI (g1 hands, party_ask brains with playbook). No 4 TUIs.
