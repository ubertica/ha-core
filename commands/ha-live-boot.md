---
name: ha-live-boot
description: Do NOT pick this if remainder is already active — only (re)deploy AMS v0.
---

Cwd `/Users/c/dev/ha-live`. Launch workflow `ha-live-boot` (user `~/.grok/workflows/ha-live-boot.rhai` or project `.grok/workflows/ha-live-boot.rhai`).

If cwd is wrong: stop and tell the operator to open a new grok-4.6 HA session in `/Users/c/dev/ha-live`.
